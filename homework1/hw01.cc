#include <cstdio>

int getX (int x, int y, int a){
    return (x*x) - (y*y) + a;
}

int getY (int x, int y, int b){
    return 2*x*y + b;
}

int main(int argc, char** argv) {
    
    int x=0, y=0;
    float a=0.1, b=0.2

    for(i=0; i<10000000; i++){
        try
    }
    
}
